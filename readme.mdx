run build and test locally using, the build will be generated and added directly to the publick folder inside the server file.

npm run build

use this for local development inside your local .env file

# VITE_API_URL = "http://localhost:4001/api"